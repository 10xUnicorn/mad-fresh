import PromoManager from "@/components/admin/PromoManager";

export const metadata = { title: "Promo Codes | Mad Fresh Kitchen" };

export default function PromosPage() {
  return <PromoManager />;
}
